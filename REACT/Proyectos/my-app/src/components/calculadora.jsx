const Calculadora = (props) =>{


    return (
    <div>
        <h2>Calculadora</h2>
        <p> El numero {props.a} + el numero {props.b} es igual a {props.a +props.b} </p>

    </div>   
    )
}

export default Calculadora